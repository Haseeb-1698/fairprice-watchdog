# FastAPI and ASGI server
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6

# Database - SQLAlchemy 2.x with async support
sqlalchemy==2.0.25
asyncpg==0.29.0
alembic==1.13.1
pgvector==0.2.4

# Redis
redis==5.0.1
hiredis==2.3.2

# Pydantic settings
pydantic==2.5.3
pydantic-settings==2.1.0

# HTTP client
httpx==0.26.0
aiohttp==3.9.1

# Environment variables
python-dotenv==1.0.0

# Evidence vault — S3-compatible client (drives MinIO locally AND Cloudflare R2)
boto3>=1.34.0
minio==7.2.3   # legacy/optional; storage.py uses boto3

# ── Agent pipeline (Haseeb) ───────────────────────────────────────────────────
requests>=2.31.0
crewai>=0.86.0            # primary orchestrator (role-based agents, max_rpm)
markitdown>=0.0.1a2       # HTML → clean markdown for the LLM
firecrawl-py>=1.2.0       # Crawler + Discovery structured extraction
anthropic>=0.39.0         # Claude Opus 4.7 LLM provider

# Optional / heavier integrations — install when building those agents:
#   playwright>=1.44.0     # Journey Simulator live browser (Bright Data Browser API)
#   skyvern                # LLM-driven multi-step checkout navigation
#   mem0ai>=0.1.0          # cross-scan agent memory
#   paddleocr  paddlepaddle # screenshot/receipt fee OCR for the evidence vault

# PDF Generation
reportlab==4.0.7

# Utilities
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-dateutil==2.8.2

# Development
pytest==7.4.4
pytest-asyncio==0.23.3
black==23.12.1
flake8==7.0.0
mypy==1.8.0